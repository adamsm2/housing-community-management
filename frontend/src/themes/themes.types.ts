export interface Palette {
  primary: string;
  primary200: string;
  secondary: string;
  background: string;
  text: string;
  highlight: string;
}

export const Theme = {
  LIGHT: "light",
  DARK: "dark",
};